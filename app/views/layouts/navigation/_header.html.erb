 <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapsible-content" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!--a class="navbar-brand" href="#">Brand</a-->
      <%= link_to 'Soccer World', root_path, class: 'navbar-brand' %>
      <% if user_signed_in? %>
      <p> <%= link_to 'New Post!', new_message_path, class:"btn btn-primary"  %>  </p>
      <% end %>
</div>

